# 为 open-gwt 做贡献

[English](CONTRIBUTING.md) · **简体中文** · [Русский](CONTRIBUTING.ru.md)

欢迎各种形式的贡献：规则、卡牌效果、对战机器人、客户端、文档以及原创美术。

## 1. 两条没有商量余地的规则

这两条用于保证项目在法律上的干净。违反其中任意一条的 PR 会被直接关闭，无需讨论。

### 不接受任何 CD PROJEKT RED 的资源

包含从昆特牌或《巫师》中提取或仿制的美术、音频、模型、卡牌名称、角色名称或风味文本的 PR 将被直接关闭。

- **不接受：** 扒取的卡面美术、音效或音乐；照搬的卡牌名、角色名、阵营名或风味文本；刻意模仿某张官方
  卡牌或某个官方角色所绘制的美术。
- **接受：** 你自己创作的原创美术和音频，以及为本项目原创撰写的名称与风味文本。

### 仅接受净室实现

根据公开的规则说明和实际游戏表现来实现功能，不要提交从官方客户端反编译或反汇编得到的代码。

- **不接受：** 反编译或反汇编得到的代码、导出的数据文件、从官方二进制文件中取得的内部标识符或常量。
- **接受：** 依据公开 wiki、更新日志、规则讲解、视频以及你自己的游玩经验写出的实现——玩法机制本身
  不受版权保护，受保护的是具体表达。

如果你不确定某件事是否越线，请**在动手写代码之前**开一个 issue 来问。

## 2. 从哪里开始

- **规则内核** —— 在与引擎无关的内核中实现卡牌与能力机制，并附带测试。
- **卡牌效果** —— 用内核提供的原语来表达单张卡牌的效果。
- **机器人** —— 会打牌的对手；它们运行在与服务端相同的内核之上。
- **客户端** —— Unity 前端：渲染、输入、交互体验、平台适配。
- **文档** —— 规则说明、客户端/服务端协议、设计决策记录。
- **美术与音频** —— 仅限原创作品，详见下方许可条款。

建议的第一步：开一个 issue 说明你想做什么，避免和别人重复劳动。

## 3. 开发约定

- **规则不要写进引擎。** 规则内核不依赖任何游戏引擎。Unity 代码只负责渲染状态和发送操作意图，
  绝不参与判定结果。
- **保持内核的确定性。** 不使用系统时间，不使用无种子的随机数，不以会影响结果的方式遍历无序集合，
  能用整数就不用浮点数。一个随机种子加一份操作记录，必须能精确复现整局对战。
- **服务端拥有最终权威。** 客户端可以做预测以提升手感，但以服务端的结果为准。隐藏信息（手牌、牌库、
  即将抽到的牌）绝不会发送给不该看到它的客户端。
- **规则改动必须带测试。** 新增卡牌效果或修复规则时，应附带一个改动前失败、改动后通过的测试。

## 4. 提交 Pull Request

1. Fork 本仓库，并从 `develop` 分支切出新分支。PR 请提交到 `develop`，不要提交到 `release`。
2. 每个 PR 只做一件事；规则改动和客户端重构应该分开提。
3. 说明**改了什么**以及**为什么改**。如果是规则改动，请说明你依据的公开资料或观察到的游戏表现。
4. 请在申请 review 之前确认构建和测试都能通过。
5. 所有 PR 都需要经过 owner 的 review 批准才能合并。两个分支都不接受直接 push。

### 分支说明

| 分支 | 用途 |
| --- | --- |
| `develop` | 集成分支。所有改动先通过 PR 合到这里。 |
| `release` | 发布分支。只有 `develop` 会在发布时合并进来。 |

发行 tag 形如 `release-1.2.3`，由 owner 在 `release` 分支上创建。完整的分支规范及其强制方式见
[`.github/BRANCH_POLICY.md`](.github/BRANCH_POLICY.md)。

## 5. 报告 Bug

规则类 bug 如果附带回放会好处理得多：请提供随机种子和操作记录，并写清你预期的结果与实际的结果。
客户端 bug 请补充平台、Unity 版本，以及截图或一小段录屏。

## 6. 贡献内容的许可

提交贡献即表示你同意：

- 你的**代码**以 [MIT 许可证](LICENSE) 发布；
- 你的**原创美术和音频**以 [CC BY 4.0](LICENSE-ASSETS) 发布，除非你在 PR 中对单个资源另行标注；
- 你拥有为所提交内容授予上述许可的权利——它是你自己的作品，或者你在其他意义上有权以上述许可贡献它。
